import type { Habit } from '../../types'
export function Score({ habit, score, muted }: { habit: Habit; score: number; muted?: boolean }) { return <div><div className="mb-2 flex items-center justify-between"><span className="flex items-center gap-2 text-sm font-semibold text-[#53655a]"><i className="h-2.5 w-2.5 rounded-full" style={{ background: habit.color }} />{habit.name}</span><span className={`text-sm font-semibold ${muted ? 'text-[#b28270]' : 'text-[#65866e]'}`}>{score}%</span></div><div className="h-2 rounded-full bg-[#edf1ec]"><div className={`h-full rounded-full ${muted ? 'bg-[#d6aa97]' : 'bg-[#91b399]'}`} style={{ width: `${score}%` }} /></div></div> }
