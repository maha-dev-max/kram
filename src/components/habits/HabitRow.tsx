import type { CSSProperties } from 'react'
import type { Habit, RecordStatus } from '../../types'

export function HabitRow({ habit, status, reason, onStatus, style }: { habit: Habit; status: RecordStatus; reason?: string; onStatus: (habit: Habit, status: 'completed' | 'skipped') => void; style?: CSSProperties }) {
  const completed = status === 'completed'
  const skipped = status === 'skipped'
  return <div style={style} className={`animate-in flex items-center gap-4 rounded-2xl border bg-white p-4 transition hover:border-[#c9d8ca] ${completed ? 'border-[#b9d8bd] bg-[#fbfefb]' : skipped ? 'border-[#ead1c5] bg-[#fffdfb]' : 'border-[#e3e9e3]'}`}><span className="h-3 w-3 shrink-0 rounded-full" style={{ background: habit.color }} /><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><p className={`truncate text-[15px] font-semibold ${completed ? 'text-[#6f8d76] line-through' : skipped ? 'text-[#9d796b]' : 'text-[#34463b]'}`}>{habit.name}</p><span className={`rounded-full px-2 py-0.5 text-[10px] font-semibold ${completed ? 'bg-[#e5f0e6] text-[#5f8568]' : skipped ? 'bg-[#f8eee8] text-[#b17c65]' : 'bg-[#f1f4f0] text-[#849188]'}`}>{completed ? 'Completed' : skipped ? 'Skipped' : 'Pending'}</span></div><p className="mt-1 text-xs text-[#96a29a]">{habit.target} · {habit.reminder}</p>{skipped && reason && <p className="mt-2 text-xs text-[#a98273]">Reason: {reason}</p>}</div><div className="flex gap-2"><button onClick={() => onStatus(habit, 'completed')} aria-label={`Mark ${habit.name} complete`} className={`rounded-xl px-3 py-2 text-xs font-semibold transition ${completed ? 'bg-[#bfdcc4] text-[#416a4b] shadow-sm ring-2 ring-[#9fc5a5]' : 'bg-[#e5f0e6] text-[#5a8263] hover:bg-[#d2e8d5]'}`}>{completed ? '✓ Completed' : 'Complete'}</button><button onClick={() => onStatus(habit, 'skipped')} aria-label={`Mark ${habit.name} skipped`} className={`rounded-xl px-3 py-2 text-xs font-semibold transition ${skipped ? 'bg-[#e8cfc2] text-[#925f4c] shadow-sm ring-2 ring-[#d6aa97]' : 'bg-[#f4f6f3] text-[#8f9d94] hover:bg-[#f8eee8] hover:text-[#b17c65]'}`}>{skipped ? '✓ Skipped' : 'Skip'}</button></div></div>
}
